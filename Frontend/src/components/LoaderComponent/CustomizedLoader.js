import React from 'react'
import Loader from "react-js-loader";

const CustomizeLoader = () => {
    return (
        <div>
            <Loader type="spinner-circle" bgColor={"#FFFFFF"} size={100} />
        </div>
    )
}

export default CustomizeLoader
